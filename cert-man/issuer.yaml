apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: issuer
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: mzeeero@gmail.com
    privateKeySecretRef:
      name: issuer
    solvers:
      - http01:
          ingress:
            class: nginx