provider:
  name: aws

env:
  - name: AWS_DEFAULT_REGION
    value: eu-west-2

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::454374565233:role/external-dns"

domainFilters:
  - lab.moabdullahi.uk

image:
  repository: registry.k8s.io/external-dns/external-dns
  tag: "v0.19.0"